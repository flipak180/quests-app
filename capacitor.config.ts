import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skav.quests',
  appName: 'quest',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
